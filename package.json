{
  "name": "amushroom",
  "version": "1.0.0",
  "private": true,
  "description": "aMushroom production web app with backend identification proxy and design tokens",
  "dependencies": {
    "better-sqlite3": "^11.9.1"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/auth.js && node --check src/db.js && node --check src/google-oauth.js && node --check public/scripts/app.js && node --check public/scripts/auth.js && node --check public/scripts/common-auth.js && node scripts/validate-tokens.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
