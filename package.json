{
  "name": "amushroom",
  "version": "1.0.0",
  "private": true,
  "description": "aMushroom production web app with backend identification proxy and design tokens",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/scripts/app.js && node scripts/validate-tokens.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
